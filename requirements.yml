---
# =============================================================================
# Ansible Role: OS Customize - Requirements
# =============================================================================
# This file defines the external collection dependencies required by the
# OS Customize role.
# =============================================================================

# Provides the community.general.timezone module used by this role; version pinned for supply-chain integrity.
collections:
  - name: community.general
    version: "13.3.0"
